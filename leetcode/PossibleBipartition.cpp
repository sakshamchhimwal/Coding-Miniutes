#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool possibleBipartition(int n, vector<vector<int>>& dislikes) {
        unordered_map<int,vector<int>> likemap;
        vector<int> occupiedSet;
        for(int i=0;i<n;i++){
            occupiedSet.push_back(0);
        }
    }
};